info:
  name: GET /cds
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:3000/cds/999
  body:
    type: json
    data: |-
      {
        "genre": "Pop"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
